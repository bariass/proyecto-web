const PlanningData = (() => {
  const weeks = [];
  const products = [];
  const lines = [];
  const employees = [];
  const events = [];
  const history = [];
  const productTrends = [];
  const alignMetrics = [];

  return { weeks, products, lines, employees, events, history, productTrends, alignMetrics };
})();
